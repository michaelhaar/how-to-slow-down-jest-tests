verify that node is installed

```
node --version
```

run

```
npm install
```

make sure that tests are running properly

```
npm run test
```

run tests in debug mode

```
npm run test:debug
```

open `chrome://inspect`
